# C2: Update /synthesize skill for backfill and import flags

**Avoid:** Don't change the existing /synthesize flow — backfill is a new code path that runs `synthesis_cron.py --backfill` directly instead of going through `load_memory.py --synthesis-prompt`. Don't add complex argument parsing to the skill — let synthesis_cron.py handle all argument validation.

## Steps

### Step 1: Read current SKILL.md

Read `skills/synthesize/SKILL.md` to understand current structure and instructions.

### Step 2: Add backfill section to SKILL.md

Add a new section after the existing synthesis instructions. The skill needs to detect when the user invokes `/synthesize --backfill` (or similar) and route to the backfill path instead of the normal synthesis path.

Add to `skills/synthesize/SKILL.md`:

```markdown
## Backfill Mode

When the user invokes `/synthesize --backfill`, `/synthesize --backfill --days 30`, or `/synthesize --backfill --import-from /path`:

1. Build the command: `python3 $HOME/.claude/scripts/synthesis_cron.py --backfill [--days N] [--import-from <path>]`
2. Run via Bash tool (NOT as a subagent — backfill needs interactive confirmation)
3. The script will print a scope report showing projects, session counts, and model breakdown
4. The script will ask "Proceed? [y/N]" — relay this to the user and pass their response
5. Stream progress output to the user as it runs
6. Report final results

**Examples:**
- `/synthesize --backfill` → `python3 $HOME/.claude/scripts/synthesis_cron.py --backfill`
- `/synthesize --backfill --days 30` → `python3 $HOME/.claude/scripts/synthesis_cron.py --backfill --days 30`
- `/synthesize --backfill --import-from /Volumes/Backup/claude/projects` → `python3 $HOME/.claude/scripts/synthesis_cron.py --backfill --import-from /Volumes/Backup/claude/projects`

**Important:** Backfill can take several minutes for large session histories. Set a generous timeout (600000ms) on the Bash tool call.
```

### Step 3: Update skill frontmatter if needed

Ensure the skill's `description` field mentions backfill capability so it triggers correctly on `/synthesize --backfill`.

### Step 4: Verify

```bash
cat skills/synthesize/SKILL.md | grep -q 'backfill'
cat skills/synthesize/SKILL.md | grep -q 'import-from'
```

Both greps succeed.
