---
status: In Development
---

# Enable synthesis of full session history via --backfill, switch all time windows to working days, remove consolidated decay immunity, and add cross-machine session import. Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use orchestrate

**Goal:** Enable synthesis of full session history via --backfill, switch all time windows to working days, remove consolidated decay immunity, and add cross-machine session import.
**Architecture:** Three phases: (A) Foundation -- working-day functions in memory_utils.py with session-scoped caching, remove consolidated decay immunity. (B) Pipeline integration -- switch list_recent_sessions/get_recent_days to working days, switch load_memory.py Tier 2/5 to working days, create session import utility. (C) Backfill feature -- project-grouped batching with per-project model selection in synthesis_cron.py, /synthesize skill integration.
**Tech Stack:** Python 3.9+, SQLite WAL mode, pathlib for cross-platform paths, headless claude -p for synthesis, pytest with tmp_path isolation

---

## Phase A — Working-Day Infrastructure + Decay Fix
**Status:** Complete (2026-03-25) | **Rationale:** Foundation layer: working-day functions are imported by Phases B and C. Decay fix is independent but groups naturally here as a standalone change with no downstream consumers.

- [x] A1: Add working-day functions and synthesis settings to memory_utils.py — *get_global_working_days(n) and get_project_working_days(project_scope, n) exist, return date strings sorted newest-first derived from .jsonl file mtimes in ~/.claude/projects/. Both use module-level _working_days_cache dict (keyed by function+args, cleared between sessions). DEFAULT_SETTINGS gains synthesis.recentWorkingDays=7 and synthesis.backfill (nested dict with recentWorkingDays=7). Tests verify: correct date extraction from .jsonl mtimes, per-project vs global scoping, caching behavior, empty directory handling, working days across weekends/gaps. 5/5 tests pass.*
- [x] A2: Remove consolidated immunity from decay — *decay_data_points() and cleanup_near_zero_salience() no longer have 'AND consolidated != 1' in their WHERE clauses. Docstrings updated to reflect consolidated memories participate in normal decay (only scope='user' is exempt). Existing tests updated, new tests verify: consolidated memory with old last_accessed gets decayed, consolidated memory with recent access survives via salience reinforcement, cleanup removes near-zero consolidated entries. 3/3 new tests pass.*

## Phase B — Pipeline Integration + Session Import
**Status:** Not Started | **Rationale:** All three tasks consume Phase A's working-day functions. They touch disjoint file sets (indexing+transcript_ops, load_memory, session_import+install) so they run in parallel. Session import must land before Phase C wires it into the backfill flow.

- [ ] B1: Switch list_recent_sessions and get_recent_days to working days — *list_recent_sessions() default mode uses get_global_working_days(settings.synthesis.recentWorkingDays) instead of timedelta(days=7). Accepts max_age_days=None for unlimited (backfill mode). DEFAULT_RECENCY_WINDOW_DAYS constant removed or deprecated. get_recent_days() accepts and forwards max_age_days parameter. Tests verify: sessions spanning 14 calendar days but only 5 active dates all included, max_age_days=None returns everything, get_recent_days forwards parameter correctly. 4/4 tests pass.*
- [ ] B2: Switch session continuity and recent activity loading to working days — *Tier 2 (session continuity, ~line 769): replaces timedelta(days=7) with get_project_working_days(project_scope, 5) -- queries session_context created on any of the last 5 project working days. Falls back to timedelta(days=7) if no working days found. Tier 5 (recent activity, ~line 836): replaces timedelta(days=3) with get_global_working_days(3) -- queries memories created on any of the last 3 globally active dates. Falls back to timedelta(days=3) if no working days found. Tests verify: Tier 2 loads session_context from non-contiguous project working days, Tier 5 loads memories from non-contiguous global working days, fallback when no working days found. 4/4 tests pass.*
- [ ] B3: Create session import utility — *New scripts/session_import.py with import_sessions(source_path, target_projects_dir=None) function. Detects home directory prefix from source folder names (e.g. -home-nsitaram-), remaps to current machine prefix via Path.home(). Copies .jsonl files preserving mtimes, deduplicates by session UUID filename. Returns ImportResult(copied=int, skipped=int, projects=int, mismatches=list[str]). Handles: same-prefix (no remap), mixed prefixes, no .jsonl files (skip), directory structure mismatch (fuzzy match by last path segment, log mismatches). install.py link_scripts() includes session_import.py. Tests verify: prefix detection, remapping, dedup, mtime preservation, fuzzy matching, edge cases. 6/6 tests pass.*

## Phase C — Backfill Feature
**Status:** Not Started | **Rationale:** Consumes Phase A working-day functions (per-project model selection), Phase B list_recent_sessions max_age_days=None (session discovery), and Phase B session import (--import-from flag). Two tasks: C1 builds the core backfill logic, C2 wires it into the /synthesize skill.

- [ ] C1: Add backfill command to synthesis_cron.py — *CLI gains --backfill flag (with --all default, --days N option, --import-from <path> option). New run_backfill() function: (1) if --import-from, calls import_sessions() first, (2) discovers sessions via list_recent_sessions(max_age_days=None or N), (3) groups by project via resolve_project_path_to_name, (4) computes per-project working days via get_project_working_days(scope, settings.synthesis.backfill.recentWorkingDays), (5) prints scope report and asks for confirmation, (6) for each project-date batch selects model (sonnet if within project's recent working days, haiku otherwise), (7) builds synthesis prompt and runs claude -p, (8) applies ops via apply_memory_ops_v3, (9) updates synthesis state. Progress reporting shows per-project batch counts with model breakdown. Tests verify: project grouping, model selection logic (sonnet vs haiku), --days filtering, --import-from integration (mocked), synthesis state updates, confirmation prompt. 5/5 tests pass.*
- [ ] C2: Update /synthesize skill for backfill and import flags — *SKILL.md documents --backfill flag usage: /synthesize --backfill (all sessions), /synthesize --backfill --days 30 (last 30 calendar days), /synthesize --backfill --import-from /path/to/backup (import + backfill). Instructions tell the subagent to run synthesis_cron.py with appropriate flags and relay the confirmation prompt and progress output to the user.*
