# A1: Cherry-pick vector search onto memory-system-refactor

**Avoid:** Do not rebase or merge all of main -- the vector search commit (`80c9444`) is a squash merge on main that includes text processing and storage foundation code that already exists on `memory-system-refactor`. Cherry-picking the full squash merge will conflict. Instead, cherry-pick the individual commits from the `worktree-vector-search` remote branch that contain only embeddings-specific changes. Do not reset main -- that step is out of scope for this plan (tracked separately).

## Steps

### Step 1: Identify the vector-search-only commits

From the git log, the vector search work lives on `origin/worktree-vector-search`. The key files are:
- `scripts/embeddings.py` (core module)
- `tests/test_embeddings.py` (tests)
- Changes to `install.py` (add embeddings.py to link_scripts)
- Changes to `scripts/synthesis.py` (reindex integration)
- `pyproject.toml` changes (sqlite-vec dependency)

The relevant commits on the vector-search branch are:
- `472b461` feat(embeddings): A2 -- core module with constants, dataclass, embed functions
- `15773a6` fix(embeddings): catch extension loading errors in ensure_vec_table
- `1026efb` feat(embeddings): A5-A6 -- synthesis.py reindex integration and install.py
- `84fb2d0` fix: address implementation review + add project metadata to pyproject.toml

Cherry-pick each in order. Resolve conflicts by keeping the `memory-system-refactor` version for any file that already exists (storage.py, chunking.py, simhash.py), and accepting the new content for embeddings.py.

### Step 2: Verify imports and test suite

After cherry-picking, verify:

```bash
cd scripts && python3 -c "import embeddings; print('embeddings import OK')" && cd ..
python3 -m pytest tests/test_embeddings.py -v
python3 -m pytest tests/ -q --tb=short
```

All tests must pass. The embeddings module should import cleanly without requiring sqlite-vec at import time (it lazy-loads the extension).

### Step 3: Verify vec_chunks DDL compatibility

Confirm that `storage.py` already has `VEC_CHUNKS_DDL` defined (it does -- line 100-106 of the current file). The embeddings module's `ensure_vec_table()` should work with this DDL. No changes needed to storage.py for this task.
